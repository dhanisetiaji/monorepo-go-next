{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "go run main.go",
    "build": "go build -o bin/main main.go",
    "start": "./bin/main",
    "test": "go test ./...",
    "clean": "rm -rf bin/"
  }
}
